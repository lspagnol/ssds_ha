# Commandes à exécuter lors de la bascule en mode "passif", juste avant
# la désactivation de l'interface virtuelle.

# Dans cet exemple: arrêt des démons Haproxy et Nginx
# Pensez à les désactiver du démarrage automatique !
# -> systemctl disable haproxy.service
# -> systemctl disable nginx.service

systemctl stop nginx.service
systemctl stop haproxy.service
